package com.community.easeim.section.voice.headphone;

public interface BluetoothAndHeadPhoneListener {
    void bluetoothHeadset(boolean connected);
    void headPhoneState(boolean connected);
}
